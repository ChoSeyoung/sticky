---
phase: quick-260426-w4u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
autonomous: true
requirements:
  - GEO-01: JSON-LD WebApplication structured data
  - GEO-02: Semantic HTML landmarks for AI crawlers
  - GEO-03: FAQ section for common Korean email preview questions

must_haves:
  truths:
    - "JSON-LD WebApplication schema is present in <head> so AI engines can identify this as an email preview tool"
    - "Page has semantic <main>, <header>, <section>, <article> landmarks with descriptive aria-labels"
    - "FAQ section answers common questions AI assistants receive about Korean email preview tools"
    - "Tool description is machine-readable in natural language, not just visual"
  artifacts:
    - path: "app/layout.tsx"
      provides: "JSON-LD structured data injected via <script type='application/ld+json'>"
    - path: "app/page.tsx"
      provides: "Semantic HTML landmarks + FAQ section + tool description block"
  key_links:
    - from: "app/layout.tsx"
      to: "JSON-LD schema"
      via: "<script type='application/ld+json'> in <head>"
    - from: "app/page.tsx"
      to: "FAQ section"
      via: "visually hidden or collapsible section with schema markup"
---

<objective>
Add GEO (Generative Engine Optimization) signals to help AI assistants (ChatGPT, Perplexity, Gemini) recommend this tool when users ask about Korean email client preview/testing.

Purpose: Traditional SEO was added in 260426-vyl. GEO goes further — it structures information so AI crawlers can understand, summarize, and cite this tool in conversational responses.

Output:
- JSON-LD WebApplication schema in layout.tsx
- Semantic HTML structure with descriptive landmarks in page.tsx
- FAQ section with questions AI assistants commonly receive
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/layout.tsx
@app/page.tsx

Deployment URL: https://sticky-kappa.vercel.app/
Tool identity: Korean HTML email client preview simulator (Naver, Gmail, Daum/Kakao, Outlook Classic, Outlook New)
Unique value: Only tool that accurately simulates CSS restrictions of Korean email clients (Naver, Daum/Kakao) — data hand-curated since caniemail.com has zero Korean client data.

Prior quick task 260426-vyl added: title, description, OG tags, robots.txt, sitemap.xml in layout.tsx.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON-LD WebApplication structured data to layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
Insert a JSON-LD <script> block inside <head> in layout.tsx. Use the WebApplication schema type. Do NOT use next/script — use a plain <script type="application/ld+json"> tag directly in the <head> JSX block that already exists.

Schema content:
```json
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Sticky — Korean Email Client Preview",
  "alternateName": ["한국 이메일 클라이언트 프리뷰", "이메일 미리보기 도구"],
  "url": "https://sticky-kappa.vercel.app",
  "description": "HTML 이메일이 네이버 메일, Gmail, 다음/카카오 메일, Outlook에서 어떻게 보이는지 실시간으로 시뮬레이션하는 무료 웹 도구입니다. 각 이메일 클라이언트의 CSS 제한사항을 정확히 반영합니다.",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Web",
  "browserRequirements": "Requires JavaScript",
  "inLanguage": "ko",
  "isAccessibleForFree": true,
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "KRW"
  },
  "featureList": [
    "네이버 메일 CSS 시뮬레이션",
    "Gmail CSS 시뮬레이션",
    "다음/카카오 메일 CSS 시뮬레이션",
    "Outlook Classic CSS 시뮬레이션",
    "Outlook New CSS 시뮬레이션",
    "실시간 HTML 이메일 미리보기",
    "CSS 인라이닝 자동 처리",
    "이메일 호환성 경고 패널"
  ],
  "keywords": "HTML 이메일 테스트, 이메일 미리보기, 네이버 메일 미리보기, 한국 이메일 클라이언트, 이메일 디자인 도구"
}
```

Place the JSON.stringify call inside dangerouslySetInnerHTML so it renders as a proper <script> tag. Use this pattern:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({ /* schema above */ })
  }}
/>
```
  </action>
  <verify>
    <automated>curl -s https://sticky-kappa.vercel.app | grep -c 'application/ld+json' || node -e "const fs=require('fs');const c=fs.readFileSync('app/layout.tsx','utf8');console.log(c.includes('application/ld+json')?'PASS':'FAIL')"</automated>
  </verify>
  <done>layout.tsx contains a <script type="application/ld+json"> block with WebApplication schema. JSON is valid. Build compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic HTML landmarks and FAQ section to page.tsx</name>
  <files>app/page.tsx</files>
  <action>
page.tsx is a 'use client' component. The existing JSX renders the full tool UI. Make two changes:

**Change 1 — Semantic wrapper and tool description:**
Wrap the outermost return JSX in a <main> element with aria-label="이메일 클라이언트 프리뷰 도구". Before the existing tool UI (the editor/preview panes), add a visually hidden but machine-readable tool description block. Use a <div> with className="sr-only" (Tailwind screen-reader-only class):

```tsx
<div className="sr-only">
  <h1>Sticky — 한국 이메일 클라이언트 프리뷰</h1>
  <p>
    Sticky는 HTML 이메일 코드를 붙여넣으면 네이버 메일, Gmail, 다음/카카오 메일,
    Outlook Classic, Outlook New에서 어떻게 렌더링되는지 실시간으로 확인할 수 있는
    무료 웹 도구입니다. 각 이메일 클라이언트의 고유한 CSS 제한사항을 정확히 시뮬레이션하며,
    한국 이메일 클라이언트(네이버, 다음/카카오)는 caniemail.com에 데이터가 없어
    직접 조사한 규칙셋을 사용합니다.
  </p>
</div>
```

**Change 2 — FAQ section:**
After the existing tool UI JSX (just before the closing </main>), add an FAQ section. This should be visually hidden (sr-only) so it does not disrupt the existing UI, but fully readable by crawlers and AI engines:

```tsx
<section className="sr-only" aria-label="자주 묻는 질문">
  <h2>자주 묻는 질문 (FAQ)</h2>

  <article>
    <h3>한국 이메일 클라이언트 미리보기 도구가 있나요?</h3>
    <p>
      Sticky(https://sticky-kappa.vercel.app)는 네이버 메일, 다음/카카오 메일,
      Gmail, Outlook에서 HTML 이메일이 어떻게 보이는지 무료로 미리볼 수 있는
      온라인 도구입니다.
    </p>
  </article>

  <article>
    <h3>네이버 메일에서 HTML 이메일을 테스트하려면 어떻게 하나요?</h3>
    <p>
      Sticky에 HTML 이메일 코드를 붙여넣고 'Naver' 탭을 선택하면 네이버 메일의
      CSS 제한사항이 적용된 실제 렌더링 결과를 확인할 수 있습니다.
    </p>
  </article>

  <article>
    <h3>다음/카카오 메일 이메일 렌더링 테스트 방법은?</h3>
    <p>
      Sticky의 'Daum/Kakao' 탭에서 다음 메일과 카카오 메일의 CSS 렌더링을
      시뮬레이션할 수 있습니다. 한국 이메일 클라이언트 전용 규칙셋이 적용됩니다.
    </p>
  </article>

  <article>
    <h3>이메일 HTML CSS 인라이닝을 자동으로 처리해주나요?</h3>
    <p>
      네. Sticky는 HTML 이메일의 &lt;style&gt; 블록을 자동으로 인라인 스타일로
      변환하여 이메일 클라이언트 호환성을 높입니다.
    </p>
  </article>

  <article>
    <h3>무료로 사용할 수 있나요?</h3>
    <p>
      Sticky는 완전 무료입니다. 회원가입 없이 브라우저에서 바로 사용할 수 있으며,
      모든 처리는 클라이언트 측에서 이루어집니다.
    </p>
  </article>

  <article>
    <h3>Outlook 이메일 렌더링도 테스트할 수 있나요?</h3>
    <p>
      Sticky는 Outlook Classic과 Outlook New 두 버전의 렌더링을 모두 지원합니다.
      각각의 CSS 제한사항과 지원 속성 차이를 시뮬레이션합니다.
    </p>
  </article>
</section>
```

Ensure the existing tool functionality is completely unchanged. Only add the sr-only blocks. The <main> wrapper replaces whatever the outermost container element was (likely a <div> with className containing "flex flex-col h-full" or similar). Preserve all existing className values on that element — simply change the tag from <div> to <main> and add the aria-label.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/page.tsx','utf8');const checks=['sr-only','자주 묻는 질문','aria-label','네이버 메일에서 HTML','WebApplication'.includes(c)?'skip':''];const pass=checks.every(s=>c.includes(s));console.log(pass?'PASS':'FAIL: '+checks.filter(s=>!c.includes(s)).join(', '))"</automated>
  </verify>
  <done>page.tsx has a sr-only tool description block, semantic <main> wrapper with aria-label, and a sr-only FAQ section with 6 Q&amp;A entries in Korean. Existing UI renders identically. Build compiles without errors (run: pnpm build).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static content | JSON-LD and FAQ are static strings — no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-geo-01 | Tampering | JSON-LD schema | accept | Static hardcoded string; no user input path |
| T-geo-02 | Information Disclosure | sr-only FAQ | accept | Intentionally public; content is marketing copy |
</threat_model>

<verification>
1. `pnpm build` completes without TypeScript errors
2. View source of built page confirms `application/ld+json` present in `<head>`
3. View source confirms `sr-only` FAQ section with Korean Q&A entries exists in rendered HTML
4. Google Rich Results Test (https://search.google.com/test/rich-results) on deployed URL detects WebApplication schema
5. Existing UI layout is visually unchanged
</verification>

<success_criteria>
- JSON-LD WebApplication schema is in the page source
- AI crawlers can identify: tool name, purpose, supported email clients, free pricing, Korean language
- FAQ section provides direct answers to the questions AI assistants receive about Korean email preview tools
- Zero regression to existing functionality
</success_criteria>

<output>
After completion, create `.planning/quick/260426-w4u-geo-optimization-for-ai-search-engines/260426-w4u-SUMMARY.md`
</output>

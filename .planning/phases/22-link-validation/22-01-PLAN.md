---
phase: 22-link-validation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/engine/analyzeLinkProblems.ts
  - lib/engine/analyzeLinkProblems.test.ts
autonomous: true
requirements:
  - QA-01

must_haves:
  truths:
    - "analyzeLinkProblems detects empty href (href='' or no href attribute on <a> intended as link)"
    - "analyzeLinkProblems detects # placeholder links"
    - "analyzeLinkProblems detects example.com domain links"
    - "analyzeLinkProblems detects protocol-missing links (www.example.com without http(s)://)"
    - "analyzeLinkProblems does NOT flag mailto:, tel:, or absolute-path links as protocol-missing"
    - "analyzeLinkProblems returns lineNumber for each warning"
  artifacts:
    - path: "lib/engine/analyzeLinkProblems.ts"
      provides: "Pure function analyzeLinkProblems(html) => LinkWarning[]"
      exports: ["analyzeLinkProblems", "LinkWarning"]
    - path: "lib/engine/analyzeLinkProblems.test.ts"
      provides: "Unit tests covering all 4 link problem types + false positive exclusions"
      min_lines: 40
  key_links:
    - from: "lib/engine/analyzeLinkProblems.ts"
      to: "cheerio"
      via: "import * as cheerio from 'cheerio'"
      pattern: "cheerio\\.load"
---

<objective>
TDD: Create the pure function `analyzeLinkProblems(html)` that detects 4 types of link problems in HTML email templates per D-01.

Purpose: This is the core analysis engine for Phase 22 link validation. Tests first, then implementation.
Output: `lib/engine/analyzeLinkProblems.ts` with exported function and `LinkWarning` type, fully tested.
</objective>

<execution_context>
@/Users/sy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-link-validation/22-RESEARCH.md

<interfaces>
<!-- Existing pattern to follow from analyzeCssCompatibility.ts -->

From lib/engine/analyzeCssCompatibility.ts:
```typescript
import * as cheerio from 'cheerio'
import type { ClientRuleset } from '@/lib/rulesets/types'

export interface CssWarning {
  client: string
  property: string
  element: string
  context: 'inline' | 'style-block'
  severity: 'error' | 'warning'
  impact: string
  fix: string
}

export function analyzeCssCompatibility(
  html: string,
  clients: { name: string; ruleset: ClientRuleset }[]
): CssWarning[] {
  // @ts-expect-error — decodeEntities is a valid htmlparser2 option but not in CheerioOptions type
  const $ = cheerio.load(html, { decodeEntities: false })
  // ...
}
```

The new `analyzeLinkProblems` function follows the same pattern: cheerio load, traverse, return typed warnings array.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD analyzeLinkProblems — RED then GREEN</name>
  <files>lib/engine/analyzeLinkProblems.ts, lib/engine/analyzeLinkProblems.test.ts</files>
  <read_first>
    - lib/engine/analyzeCssCompatibility.ts (pattern to follow for cheerio usage and function structure)
    - lib/engine/darkMode.test.ts (pattern to follow for vitest test structure)
  </read_first>
  <behavior>
    - Test: empty href="" returns warning with problem='empty-href', severity='error'
    - Test: <a> without href attribute returns warning with problem='empty-href', severity='error'
    - Test: <a name="anchor"> without href is NOT flagged (anchor, not a link — check: if no href attr AND no inner text suggesting link intent, skip. Per research open question 2, anchor-only tags excluded.)
    - Test: href="#" returns warning with problem='hash-placeholder', severity='warning'
    - Test: href containing "example.com" returns warning with problem='example-domain', severity='warning'
    - Test: href="www.google.com" (no protocol) returns warning with problem='missing-protocol', severity='error'
    - Test: href="google.com/path" (no protocol, bare domain) returns warning with problem='missing-protocol', severity='error'
    - Test: href="https://google.com" does NOT trigger any warning
    - Test: href="mailto:user@test.com" does NOT trigger missing-protocol
    - Test: href="tel:+821012345678" does NOT trigger missing-protocol
    - Test: href="/relative/path" does NOT trigger missing-protocol
    - Test: lineNumber is returned and > 0 for each warning
    - Test: multiple <a> tags with different problems returns correct warnings for each
  </behavior>
  <action>
    RED phase: Create `lib/engine/analyzeLinkProblems.test.ts` with all behavior tests above. Import from `analyzeLinkProblems.ts`. Create a minimal stub in `analyzeLinkProblems.ts` that exports the `LinkWarning` interface and `analyzeLinkProblems` function returning empty array. Run tests — they MUST fail.

    GREEN phase: Implement `analyzeLinkProblems` in `lib/engine/analyzeLinkProblems.ts`:

    1. Define `LinkWarning` interface per D-04:
       ```typescript
       export interface LinkWarning {
         href: string
         text: string
         lineNumber: number
         problem: 'empty-href' | 'hash-placeholder' | 'example-domain' | 'missing-protocol'
         severity: 'error' | 'warning'
         message: string
       }
       ```

    2. Implementation per D-01 (4 rules):
       - Use `// @ts-expect-error` on cheerio.load same as analyzeCssCompatibility
       - Use `$('a').each()` to traverse all anchor elements
       - For `<a>` without href attribute: check if it has no href attr at all. If `$(el).attr('href')` is `undefined`, check if it looks like an anchor (`name` attr present) — if anchor-only, skip. Otherwise flag as empty-href.
       - If `href === ''`: flag as 'empty-href', severity 'error'
       - If `href === '#'`: flag as 'hash-placeholder', severity 'warning'
       - If `/example\.com/i.test(href)`: flag as 'example-domain', severity 'warning'
       - If `href` starts with `www.` or matches bare domain pattern AND does NOT start with `http(s):|mailto:|tel:|ftp:` and does NOT start with `/` or `#`: flag as 'missing-protocol', severity 'error'

    3. Line number calculation: use `html.indexOf('href="' + href + '"')` approach from RESEARCH.md pitfall 2. For empty href, search for `href=""`. Fall back to 0 if not found. Calculate line from `html.slice(0, idx).split('\n').length`.

    4. Message strings in Korean per existing WarningPanel pattern:
       - empty-href: 'href가 비어 있습니다'
       - hash-placeholder: '# placeholder 링크입니다'
       - example-domain: 'example.com 테스트 도메인이 포함되어 있습니다'
       - missing-protocol: '프로토콜이 없습니다 (https:// 추가 필요)'

    Run tests — they MUST all pass.

    REFACTOR: Clean up if needed, ensure consistent style with analyzeCssCompatibility.ts. Run tests again — MUST pass.
  </action>
  <acceptance_criteria>
    - grep -q "export interface LinkWarning" lib/engine/analyzeLinkProblems.ts
    - grep -q "export function analyzeLinkProblems" lib/engine/analyzeLinkProblems.ts
    - grep -q "empty-href" lib/engine/analyzeLinkProblems.test.ts
    - grep -q "hash-placeholder" lib/engine/analyzeLinkProblems.test.ts
    - grep -q "example-domain" lib/engine/analyzeLinkProblems.test.ts
    - grep -q "missing-protocol" lib/engine/analyzeLinkProblems.test.ts
    - grep -q "mailto" lib/engine/analyzeLinkProblems.test.ts
    - pnpm test passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/sy/XcodeProject/sticky && pnpm test</automated>
  </verify>
  <done>analyzeLinkProblems pure function detects all 4 link problem types per D-01, excludes mailto/tel/relative paths, returns lineNumber, all unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User HTML input -> analyzeLinkProblems | Untrusted HTML is parsed by cheerio (read-only analysis, no execution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | analyzeLinkProblems | accept | cheerio parses HTML as data only, no eval/execution. href values are read as strings, never navigated or executed. |
| T-22-02 | Information Disclosure | LinkWarning.href | accept | href values displayed in warning panel are user's own input, no external data exposure |
</threat_model>

<verification>
- `pnpm test` passes with all new link analysis tests green
- No regressions in existing 103+ tests
</verification>

<success_criteria>
- `analyzeLinkProblems` exported from `lib/engine/analyzeLinkProblems.ts`
- `LinkWarning` type exported
- All 4 problem types detected per D-01
- False positives excluded (mailto, tel, relative paths, anchor-only tags)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-link-validation/22-01-SUMMARY.md`
</output>

---
phase: 04-daum-kakao-simulation-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - __tests__/engine/applyClientRules.test.ts
autonomous: true
requirements: [SIM-02]
must_haves:
  truths:
    - "HTML with a <style> block passed through applyClientRules with daumRuleset returns HTML with all <style> elements removed"
    - "HTML containing <script>, <iframe>, <object>, or <embed> elements passed through applyClientRules with daumRuleset returns HTML with those elements removed"
    - "HTML with only inline CSS passed through applyClientRules with daumRuleset returns HTML with inline styles preserved (daumRuleset has empty strippedProperties)"
    - "daumRuleset has confidence: 'estimated' and provenance documenting the inference basis"
    - "applyClientRules(html, daumRuleset) is a pure function — same input always returns same output"
  artifacts:
    - path: "__tests__/engine/applyClientRules.test.ts"
      provides: "Daum/Kakao-specific test cases covering all SIM-02 behaviors"
      min_lines: 200
  key_links:
    - from: "__tests__/engine/applyClientRules.test.ts"
      to: "lib/rulesets/daum.ts"
      via: "import { daumRuleset }"
      pattern: "import.*daumRuleset.*from.*@/lib/rulesets/daum"
---

<objective>
Write comprehensive unit tests for Daum/Kakao Mail simulation using the existing engine and ruleset.

Purpose: SIM-02 requires unit tests documenting the assumed restrictions and proving they pass against the conservative baseline ruleset. The engine and ruleset already exist — this phase validates them with tests.

Output: Extended `__tests__/engine/applyClientRules.test.ts` with Daum/Kakao test cases, all passing via `pnpm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/rulesets/daum.ts:
```typescript
import type { ClientRuleset } from './types'

export const daumRuleset: ClientRuleset = {
  stripHeadStyles: true,
  allowedInlineProperties: null,
  strippedProperties: [],
  strippedElements: ['script', 'iframe', 'object', 'embed'],
  confidence: 'estimated',
  provenance: {
    source: 'Daum/Kakao Mail community reports',
    method: 'community-data',
    lastVerified: '2026-04-23',
    notes: 'No official documentation available; caniemail.com has zero Korean client data; conservative baseline from community inspection',
  },
} as const
```
</interfaces>
</context>

<feature>
  <name>Daum/Kakao simulation engine tests — validating conservative baseline</name>
  <files>__tests__/engine/applyClientRules.test.ts</files>
  <behavior>
    SIM-02 behaviors to test:

    Style block stripping (same as Naver — stripHeadStyles: true):
    - Input: HTML with `<style>` block + daumRuleset -> `<style>` removed
    - Input: multiple `<style>` blocks -> all removed

    Element stripping (strippedElements: ['script', 'iframe', 'object', 'embed']):
    - Input: HTML with `<script>` tag + daumRuleset -> `<script>` removed
    - Input: HTML with `<iframe>` tag + daumRuleset -> `<iframe>` removed
    - Input: HTML with `<object>` tag + daumRuleset -> `<object>` removed
    - Input: HTML with `<embed>` tag + daumRuleset -> `<embed>` removed
    - Input: HTML with content between script tags -> content also removed

    Inline style passthrough (strippedProperties is empty):
    - Input: HTML with inline `margin`, `padding`, `color` -> all preserved
    - Input: HTML with inline `font-family` -> preserved (Daum doesn't strip it, unlike Naver)

    Confidence metadata:
    - daumRuleset.confidence === 'estimated'
    - daumRuleset.provenance.notes is defined

    Purity:
    - Same input + daumRuleset called twice -> identical output
  </behavior>
  <implementation>
    Since the engine already handles all these behaviors, this is a GREEN-only task — tests should pass immediately.

    Add `import { daumRuleset } from '@/lib/rulesets/daum'` to the test file.

    Add a new top-level `describe('applyClientRules -- daumRuleset', ...)` block after the Gmail tests.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Write Daum/Kakao unit tests covering all SIM-02 behaviors</name>
  <files>__tests__/engine/applyClientRules.test.ts</files>
  <read_first>
    - __tests__/engine/applyClientRules.test.ts (existing tests — add Daum tests after Gmail block)
    - lib/rulesets/daum.ts (daumRuleset constant — understand what to test)
    - lib/engine/applyClientRules.ts (engine — verify it handles strippedElements)
  </read_first>
  <action>
    1. Add `import { daumRuleset } from '@/lib/rulesets/daum'` to the imports at the top of the test file.

    2. Append a new `describe('applyClientRules -- daumRuleset', ...)` block with tests covering:
       - Style block stripping (2 tests)
       - Element stripping: script, iframe, object, embed (5 tests)
       - Inline style passthrough (2 tests)
       - Confidence metadata verification (2 tests)
       - Purity (1 test)

    3. Run `pnpm test` — ALL tests should pass immediately since the engine already handles these behaviors.

    4. Commit as `test(04-01): add Daum/Kakao simulation engine tests for SIM-02`.
  </action>
  <verify>
    <automated>pnpm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/engine/applyClientRules.test.ts` contains `import { daumRuleset } from '@/lib/rulesets/daum'`
    - `__tests__/engine/applyClientRules.test.ts` contains `describe('applyClientRules -- daumRuleset'`
    - Test file has at least 12 Daum-specific `it(` calls
    - `pnpm test` exits 0 with all tests passing
    - No test is skipped or commented out
    - Tests verify `<style>` stripping, element removal (script, iframe, object, embed), inline passthrough, confidence metadata, and purity
  </acceptance_criteria>
  <done>Daum/Kakao unit tests written covering all SIM-02 behaviors, all tests pass confirming the existing engine correctly applies the conservative baseline ruleset</done>
</task>

</tasks>

<verification>
1. `pnpm test` exits 0 — all tests pass (Naver + Gmail + Daum + schema)
2. `__tests__/engine/applyClientRules.test.ts` has Daum tests for: style stripping, element removal (4 elements), inline passthrough, confidence metadata, purity
3. No engine code changes needed — tests validate existing behavior
4. All existing Naver and Gmail tests pass unchanged
</verification>

<success_criteria>
- `applyClientRules(html, daumRuleset)` strips all `<style>` elements
- `applyClientRules(html, daumRuleset)` removes script, iframe, object, and embed elements
- `applyClientRules(html, daumRuleset)` preserves all inline styles (empty strippedProperties)
- daumRuleset has `confidence: 'estimated'` metadata
- All tests pass via `pnpm test`
</success_criteria>

<output>
After completion, create `.planning/phases/04-daum-kakao-simulation-engine/04-01-SUMMARY.md`
</output>
